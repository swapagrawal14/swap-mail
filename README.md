# SwapMail

A responsive temporary-email inbox interface with live public-domain loading, address generation, inbox interactions, and light/dark themes.

## Run locally

```bash
python3 -m http.server 8000 --bind 0.0.0.0
```

Open `http://localhost:8000`.
